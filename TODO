ask const discard error nand_connect_signal !!

when dont have an input EINVAL or ECANCELED
detect cycles in gates 
buffer gate ouputs
if theres no input we fail

move static declarations to c files
add all declarations
function naming convention -> add nand everywhere
makefile and linking...

HOW:
test memory